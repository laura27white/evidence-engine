{
  "name": "@tp/intelligence",
  "version": "0.0.1",
  "private": true,
  "description": "Pure-function forecast, cascade, and confidence intelligence layer for Project Trueplan.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.{ts,tsx}\" --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "build": "tsc"
  }
}
